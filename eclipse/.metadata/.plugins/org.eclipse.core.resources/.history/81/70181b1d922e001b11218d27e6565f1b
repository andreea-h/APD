
public class Task extends Thread {
	private int id;
	
	public Task(int id) {
		this.id = id;
	}
	
	public void run() {
		int start = id * (int) Math.ceil((double) Main.N / Main.NUM_THREADS);
		int end = Math.min(size, (id + 1) * (int) Math.ceil((double) size / noThreads));
		
		
	}
}

